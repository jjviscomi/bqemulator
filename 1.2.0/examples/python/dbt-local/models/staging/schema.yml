version: 2

models:
  - name: stg_customers
    description: "Cleaned customer rows from the seed."
    columns:
      - name: customer_id
        description: "Primary key."
        tests:
          - not_null
          - unique
      - name: customer_name
        tests:
          - not_null
      - name: email
        tests:
          - not_null
