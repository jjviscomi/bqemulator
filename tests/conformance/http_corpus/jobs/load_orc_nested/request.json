{
  "method": "POST",
  "path": "/bigquery/v2/projects/${PROJECT}/jobs",
  "body": {
    "configuration": {
      "load": {
        "destinationTable": {
          "projectId": "${PROJECT}",
          "datasetId": "${DATASET_ID}",
          "tableId": "people"
        },
        "sourceUris": ["gs://${GCS_BUCKET}/g1/load_orc_nested.orc"],
        "sourceFormat": "ORC",
        "writeDisposition": "WRITE_TRUNCATE"
      }
    }
  }
}
