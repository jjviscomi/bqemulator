[
  {
    "method": "POST",
    "path": "/bigquery/v2/projects/${PROJECT}/jobs",
    "body": {
      "configuration": {
        "extract": {
          "sourceTable": {
            "projectId": "${PROJECT}",
            "datasetId": "${DATASET_ID}",
            "tableId": "src"
          },
          "destinationUris": ["gs://${GCS_BUCKET}/g1/extract_avro_round_trip.avro"],
          "destinationFormat": "AVRO"
        }
      }
    }
  }
]
