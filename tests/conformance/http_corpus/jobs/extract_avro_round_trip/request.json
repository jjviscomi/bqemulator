{
  "method": "POST",
  "path": "/bigquery/v2/projects/${PROJECT}/jobs",
  "body": {
    "configuration": {
      "load": {
        "destinationTable": {
          "projectId": "${PROJECT}",
          "datasetId": "${DATASET_ID}",
          "tableId": "dst"
        },
        "sourceUris": ["gs://${GCS_BUCKET}/g1/extract_avro_round_trip.avro"],
        "sourceFormat": "AVRO",
        "writeDisposition": "WRITE_TRUNCATE"
      }
    }
  }
}
