# dbt-bigquery 1.9 vendors a protobuf 6.x-compatible gRPC stub *and*
# emits the right two-part ``project.dataset`` identifier in DDL.
# 1.10 introduced a regression that emits
# ``CREATE SCHEMA "{project}__{dataset}_{custom_schema}"."" `` —
# the database carries the full triple, the schema is empty —
# which is malformed SQL and bqemulator's parser rejects.
# Pin to 1.9 until upstream lands a fix (or v1.0.1 patches the
# parser to tolerate the alternate shape).
dbt-core>=1.9,<1.10
dbt-bigquery>=1.9,<1.10
